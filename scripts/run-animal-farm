#!/bin/zsh
set -eu
PROJECT_DIR="/Users/gt/Developer/animal-farm"
LOG_DIR="$PROJECT_DIR/logs"
mkdir -p "$LOG_DIR"
cd "$PROJECT_DIR"
exec "$PROJECT_DIR/.venv/bin/python" -m uvicorn app.main:app --host 127.0.0.1 --port "${ANIMAL_FARM_PORT:-8002}"
